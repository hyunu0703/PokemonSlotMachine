현재 Pokemon Slot Machine 웹 프로젝트의 시장 가격 History 저장 구조를 최적화해줘.

먼저 기존 프로젝트 전체를 확인하고 특히 다음 파일과 연결 관계를 분석해.

* `js/market.js`
* `js/storage.js`
* `js/market-view.js`
* `js/app.js`
* 시장 데이터를 저장하는 기존 localStorage 구조

기존 기능을 최대한 유지하고, 필요한 파일만 최소한으로 수정해.

## 목적

현재 시장 가격은 10분마다 변경된다.

가격 기록이 계속 배열에 누적되어 localStorage 저장 용량이 계속 증가하는 문제를 해결하고 싶다.

시장 가격 계산 주기는 기존처럼 10분을 유지한다.

가격 History만 고정 크기로 관리한다.

---

## 1. 최근 24시간 History

각 포켓몬마다 최근 24시간의 가격을 10분 단위로 저장한다.

* 1시간 = 6개
* 24시간 = 최대 144개
* 최대 144개를 초과하지 않는다.
* 새로운 가격이 들어오면 가장 오래된 가격을 덮어쓴다.
* `shift()`를 반복하는 배열 방식보다 고정 크기 순환 버퍼 방식으로 구현한다.
* 데이터 추가는 O(1)이어야 한다.

필요한 상태 예시:

```js
{
    values: [],
    head: 0,
    count: 0
}
```

단, 기존 프로젝트 구조에 더 적합한 형태가 있다면 불필요하게 클래스를 추가하지 말고 단순하게 구현해.

---

## 2. 최근 7일 History

7일 데이터는 10분 데이터를 모두 저장하지 않는다.

1시간마다 가격 하나만 기록한다.

* 1일 = 24개
* 7일 = 최대 168개
* 최대 168개를 초과하지 않는다.
* 새로운 데이터가 들어오면 가장 오래된 데이터를 덮어쓴다.
* 순환 버퍼 사용
* 추가 O(1)

시장 가격 자체는 여전히 10분마다 계산한다.

즉:

```text
10분
→ 가격 계산
→ 24시간 History 기록

6번째 10분 Tick = 1시간
→ 7일 History에도 기록
```

단순 Tick 횟수보다 기존 timestamp 구조를 이용하는 것이 안전하다면 기존 시간 시스템을 활용해.

페이지를 오래 닫았다 다시 실행했을 때 수행되는 기존 Offline/Catch-up 시장 계산도 정상적으로 동작해야 한다.

---

## 3. 장기 통계

7일보다 오래된 가격 History 자체는 저장하지 않는다.

대신 각 포켓몬마다 다음 장기 통계만 유지한다.

```text
역대 최고가
역대 최저가
역대 평균가
평균 계산에 필요한 누적 데이터
```

모든 과거 가격 배열을 저장해서 평균을 다시 계산하면 안 된다.

가격이 변경될 때 O(1)로 갱신한다.

예:

```text
sampleCount
averagePrice
```

또는

```text
sampleCount
totalPrice
```

중 기존 구조에 더 적합한 방법을 선택해.

숫자가 지나치게 커지는 문제가 있다면 누적 합계 대신 incremental average 방식을 검토해.

---

## 4. 저장 용량

게임을 7일, 30일, 1년 실행해도 History 데이터 크기가 계속 증가하면 안 된다.

포켓몬 1마리 기준 최대 History는:

```text
24시간 History = 144개
7일 History = 168개
```

즉 최대 312개의 History 가격만 유지한다.

현재 가격은 별도로 1개만 유지한다.

---

## 5. 기존 저장 데이터 호환

현재 사용자의 기존 localStorage 세이브가 가능한 한 깨지지 않도록 처리해.

기존 History 데이터가 있다면:

* 최근 24시간에 필요한 데이터만 가져오기
* 7일 데이터로 변환 가능한 경우 필요한 값만 변환
* 초과된 오래된 History 제거

기존 저장 구조와 호환이 어렵다면 save version을 추가하는 방식을 검토해.

데이터 초기화를 강제로 요구하는 방식은 가능하면 사용하지 않는다.

---

## 6. 차트 표시

순환 버퍼 내부 배열 순서는 실제 시간 순서와 다를 수 있다.

`market-view.js`가 차트를 표시할 때는 반드시:

```text
가장 오래된 가격
→
가장 최신 가격
```

순서로 데이터를 받을 수 있도록 해.

UI 코드가 순환 버퍼의 `head` 계산을 직접 알 필요가 없도록 데이터 반환 책임을 시장 데이터 쪽에 두는 것을 우선 검토해.

---

## 7. 성능 규칙

가격 History 추가:

* O(1)

최고가 갱신:

* O(1)

최저가 갱신:

* O(1)

평균가 갱신:

* O(1)

History 추가 과정에서:

* `shift()`
* 전체 History 재계산
* 전체 배열 복사

같은 작업을 매 Tick마다 하지 않는다.

차트 표시처럼 실제 전체 데이터를 읽어야 하는 경우에는 O(n)을 허용한다.

---

## 8. 기존 기능 유지

다음 기능을 깨뜨리지 않는다.

* 10분 시장 Tick
* Offline 시장 진행
* 현재 가격
* 급등 / 급락
* Momentum
* 변동성
* 시장 상태
* 희귀도 시장 상태
* 뉴스
* 카드 매도
* Market UI
* 기존 저장 / 불러오기

시장 가격 계산 공식 자체는 이번 작업에서 변경하지 않는다.

---

## 9. 구현 방식

바로 코드를 수정하기 전에 먼저 짧게 보고해.

1. 현재 History 저장 구조
2. 어떤 파일을 수정할 것인지
3. 새 데이터 구조
4. 기존 저장 데이터 Migration 방법
5. 데이터 흐름

그 다음 구현해.

기존 코드로 해결할 수 있으면 새 스크립트를 만들지 않는다.

불필요한 클래스, 함수, 변수, 주석을 추가하지 않는다.

기능 구현 후 같은 기능을 유지하면서 코드량을 더 줄일 수 있는지 다시 검토해.

---

## 10. 테스트

구현 후 최소한 다음을 확인해.

* 24시간 History가 144개를 넘지 않는지
* 7일 History가 168개를 넘지 않는지
* 145번째 10분 데이터가 들어왔을 때 가장 오래된 데이터가 정상적으로 교체되는지
* 169번째 시간 데이터가 들어왔을 때 정상적으로 교체되는지
* 차트 시간 순서가 올바른지
* 최고가가 정상 갱신되는지
* 최저가가 정상 갱신되는지
* 평균가가 정상 갱신되는지
* Offline Catch-up 후에도 크기가 제한되는지
* 저장 후 새로고침해도 순환 버퍼 상태가 유지되는지
* 기존 세이브 데이터 로드가 가능한지

가능하면 기존 테스트 구조를 재사용해.

작업 완료 후:

* 수정한 파일
* 핵심 변경 사항
* 테스트 결과
* 남아 있는 문제

만 간단히 보고해.
