# MARKET 구현 결과

1. **수정 파일**
   - `index.html`: MARKET 메뉴, 자산/상세/뉴스/종목 목록, TC 표시, 슬롯 비용, 초기화 안내.
   - `css/style.css`: 기존 파스텔·둥근 패널 스타일을 확장한 시장 화면과 모바일 레이아웃, 보유량 배지.
   - `js/app.js`: 기존 화면 전환에 시장 연결, TC 결제·수집·판매·저장, 경과 Tick 처리.
   - `js/slot.js`: 전체 희귀도 Pool에서 중복 획득, 비용 차감 연결. 기존 확률·릴 애니메이션·당첨 연출 유지.
   - `js/collection.js`: 영구 도감 등록과 별개로 현재 보유량 표시.
   - `README.md`: 실행, 게임 규칙, 저장 이전, 테스트 방법 안내.

2. **새 파일과 역할**
   - `js/market.js`: 시장/뉴스 시뮬레이션, TC·보유량·자산·판매 계산. 시간과 난수 주입을 지원하는 순수 로직.
   - `js/market-view.js`: 25개 단위 종목 페이지네이션, 검색/희귀도/정렬, SVG 차트, 카드 상세, 매도, 뉴스 상세.
   - `js/storage.js`: 기존 저장 함수 분리, version 1 → 2 데이터 이전 및 기본값 검증.
   - `tests/market.test.mjs`: 시장·경제·이전 로직 11개 테스트 그룹.
   - `tests/browser.test.cjs`: 별도 포트와 임시 브라우저를 사용하는 실제 브라우저 검증.
   - `tests/market-desktop.png`, `tests/market-history.png`, `tests/market-mobile.png`: 실제 렌더링 검증 이미지.
   - `MARKET_IMPLEMENTATION.md`: 이 보고서.

3. **기존 구조 재사용**
   - HTML + CSS + ES 모듈 구조와 로컬 1,025종 데이터/이미지를 유지.
   - 기존 `collectedIds`와 런타임 `Set`을 discovered 상태로 재사용. 별도의 `quantity[id]`로 보유 수량 저장.
   - 프레임워크, 서버 API, 로그인, DB, 카드 매수는 추가하지 않음. Git push 없음.

4. **슬롯 알고리즘**
   - 일반 1,000 TC / 50%, 전설 25,000 TC / 20%, 환상 150,000 TC / 10%.
   - 이미 발견한 카드도 전체 Pool에 포함. 기존 균등 후보 추출과 당첨 카드 선택 방식 재사용.
   - 시작 시 TC 검사·차감. 당첨 결과는 비동기 연출 전에 저장하여 새로고침으로 보상이 사라지지 않도록 처리.
   - 중복 당첨은 수량 증가, 전체 판매 후에도 도감 유지. 초기 지급 500,000 TC는 조정 가능한 상수.

5. **시장 알고리즘**
   - 초기 희귀도 평균 가격을 2,000 / 125,000 / 1,500,000 TC에 정규화.
   - Date.now 기준 10분 Tick, 남은 분 단위 시간 보존, 음수 Tick 방지, 오프라인 경과분 처리.
   - 전체/희귀도별 NORMAL·BULL·BEAR·SIDEWAYS 상태가 6~30 Tick 지속.
   - 카드별 Trend·Momentum·변동성, 시장 및 뉴스 편향을 반영.
   - 일반 급등은 70/20/8/2% 가중 구간, 최대 +500%. 등급별 급락/급등 범위 준수.
   - 괴리가 30%를 넘는 경우에만 18% 확률로 현재 FairPrice를 향한 복원 후보 적용. 급등락 Tick에는 복원으로 범위를 넘기지 않음.
   - FairPrice는 시장 편향과 지속 가격대를 천천히 반영. StartingPrice는 최초 기준으로 유지.
   - Tick은 종목 수에 대해 O(n). 긴 경과분은 36 Tick씩 처리하며 이벤트 루프에 제어권을 돌려줌.

6. **뉴스 알고리즘**
   - PC 현지 시간 08시·12시·18시, 각 시간대 65% 발생 확률.
   - 전체/희귀도/개별 카드, 호재/악재/중립, 6~12 Tick 지속 및 선형 감쇠.
   - 가격 직접 변경 없이 방향 편향·변동성·급등락 발생/방향 확률·Momentum 영향에 반영.
   - 여러 뉴스의 효과 합산과 상쇄 지원. 활성 News ID 중복 방지, 처리한 시간대 저장.

7. **저장**
   - 기존 localStorage 키 `pokemonSlotSaveV1` 유지, 내부 version 2.
   - 기존 발견 카드 각 1장으로 이전, 설정 유지. 수량 0도 정상 저장/복원.
   - TC, 발견 ID, 수량, 시장/카드 상태, 가격 기록, 활성 뉴스/이력, 마지막 갱신 시각 저장.
   - 가격 기록 145개, 뉴스 이력 40개 제한. 저장 실패 시 기존 토스트로 안내.

8. **테스트 결과**
   - Node 계산 테스트 11개 그룹 통과.
   - 실제 Edge(Chromium)에서 초기 로드, 메뉴 전환, 일반 중복 당첨과 연출 종료, 실패 시 차감, 전설/환상 비용 확인.
   - 1장/전체 판매의 정확한 TC, 도감 유지, 보유량 0, 새로고침 복원 확인.
   - 전체 등급 TC 부족 차단, 기존 version 1 이전, 필터/검색/정렬/페이지네이션/차트 기간 확인.
   - 37분 오프라인 → 3 Tick, 2일 경과 처리, 기록 145개 제한 확인.
   - PC 및 390px 모바일 스크린샷 직접 확인. 페이지 가로 넘침 없음, 브라우저 콘솔 오류 없음.
   - 1,025종 × 200 Tick 계산 약 146~155ms, 해당 저장 크기 UTF-16 기준 약 1.94MiB.
   - JS 구문 검사와 `git diff --check` 통과.

9. **남아 있는 제한**
   - 확인된 기능 오류 없음. 장기간의 게임 경제 균형은 추가 플레이에 따라 조정할 수 있음.
   - ALL은 무제한 과거 기록이 아니라 보관된 최근 최대 24시간. 아직 쌓이지 않은 기간의 등락률은 ‘기록 부족’ 표시.
   - 시작 가격 기준 기대값은 100%이나 시장 가격이 변하면 현재 시점 슬롯 기대값도 달라짐.
   - 무료 재지급 기능은 없음. TC와 보유 카드가 모두 소진되면 설정에서 게임 데이터를 초기화해 다시 시작 가능.

10. **실행 방법**
    - 기존 `Start-Server.cmd` 더블 클릭 → `http://127.0.0.1:4173`.
    - 계산 검증: `node tests/market.test.mjs`.
    - 브라우저 검증: Playwright 설치 환경에서 `node tests/browser.test.cjs`. 외부 설치는 `PLAYWRIGHT_MODULE` 환경변수로 패키지 폴더 지정. 기본 Edge, 다른 채널은 `BROWSER_CHANNEL`로 지정.
    - 브라우저 테스트는 포트 4174의 임시 세션에서만 수행하므로 실제 사용자 저장을 변경하지 않음.
